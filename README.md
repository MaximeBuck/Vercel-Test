# Banana Weather Report

A tiny static site that shows a random silly forecast each time you click the button.

## Run locally

Open `index.html` directly, or run a static server:

```bash
python3 -m http.server 4173
```

Then visit <http://localhost:4173>.

## Deploy on Vercel

This repo is already Vercel-ready as a static site.
